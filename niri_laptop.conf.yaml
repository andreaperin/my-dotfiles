- defaults:
    link:
      relink: true

- clean: ["~/.config/niri"]

- link:
    ~/.config/niri/config.kdl: niri/config_laptop.kdl

- shell:
    - [git submodule update --init --recursive, Installing submodules]
