- defaults:
    link:
      relink: true

- clean: ["~/.config/fuzzel"]

- link:
    ~/.config/fuzzel/fuzzel.ini: fuzzel/fuzzel.ini

- shell:
    - [git submodule update --init --recursive, Installing submodules]
