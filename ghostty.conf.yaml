- defaults:
    link:
      relink: true

- clean: ["~/.config/ghostty"]

- link:
    ~/.config/ghostty/config: ghostty/ghostty_config

- shell:
    - [git submodule update --init --recursive, Installing submodules]
